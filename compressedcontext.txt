=== PROJECT HIERARCHY ===
├── .aiignore
├── .github
│   └── workflows
│       └── deploy.yml
├── .gitignore
├── .vscode
│   └── launch.json
├── main.py
├── requirements.txt
├── static
│   └── sync.bat
└── templates
    ├── author.html
    ├── commands.html
    ├── index.html
    └── work.html

=== SOURCE CODE ===

--- FILE: main.py ---
from fastapi import FastAPI, Request
from fastapi.templating import Jinja2Templates
from fastapi.staticfiles import StaticFiles

app = FastAPI()

app.mount("/static", StaticFiles(directory="static"), name="static")
templates = Jinja2Templates(directory="templates")

@app.get("/")
async def index(request: Request):
    return templates.TemplateResponse("index.html", {"request": request})

@app.get("/author")
async def author(request: Request):
    return templates.TemplateResponse("author.html", {"request": request})

@app.get("/work")
async def work(request: Request):
    return templates.TemplateResponse("work.html", {"request": request})

@app.get("/commands")
async def commands(request: Request):
    return templates.TemplateResponse("commands.html", {"request": request})

--- FILE: templates/index.html ---
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Сайт Павла Забелича</title>
    <style>
        body { font-family: sans-serif; max-width: 800px; margin: 50px auto; line-height: 1.6; padding: 0 20px; }
        nav { background: #f4f4f4; padding: 10px; margin-bottom: 20px; border-radius: 5px; }
        nav a { margin-right: 15px; text-decoration: none; color: #007bff; font-weight: bold; }
        h1 { color: #333; }
    </style>
</head>
<body>
    <nav>
        <a href="/">Главная</a>
        <a href="/author">Об авторе</a>
        <a href="/work">О работе сайта</a>
        <a href="/commands">Команды</a>
    </nav>

    <h1>Сайт Павла Забелича</h1>
    <p>Добро пожаловать на мой домашний сервер, запущенный на Orange Pi Zero 3!</p>
    <p>Этот сайт обновляется автоматически через GitHub Actions (CI/CD).</p>
</body>
</html>

--- FILE: templates/commands.html ---
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Полезные команды — Мой сервер</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; max-width: 900px; margin: 40px auto; line-height: 1.6; color: #333; padding: 0 20px; }
        nav { background: #333; padding: 15px; border-radius: 8px; margin-bottom: 30px; }
        nav a { color: white; margin-right: 20px; text-decoration: none; font-weight: 500; }
        nav a:hover { color: #00d4ff; }
        
        .command-card { background: #f9f9f9; border-left: 5px solid #0078d4; padding: 20px; margin-bottom: 20px; border-radius: 0 8px 8px 0; box-shadow: 0 2px 5px rgba(0,0,0,0.05); }
        h2 { margin-top: 0; color: #0078d4; font-size: 1.2rem; }
        code { background: #2d2d2d; color: #f8f8f2; padding: 10px; display: block; border-radius: 5px; margin: 10px 0; overflow-x: auto; font-family: 'Consolas', monospace; }
        .description { font-size: 0.95rem; color: #666; }
    </style>
</head>
<body>
    <nav>
        <a href="/">Главная</a>
        <a href="/author">Об авторе</a>
        <a href="/work">О работе</a>
        <a href="/commands">Команды</a>
    </nav>

    <h1>Шпаргалка по командам</h1>

    <div class="command-card">
        <h2>Просмотр логов сервиса  </h2>
        <p class="description">Позволяет в реальном времени видеть, что происходит с твоим сайтом и почему он может не запускаться.</p>
        <code>sudo journalctl -u service -f</code>
    </div>

    <div class="command-card">
        <h2>Перезапуск сервиса</h2>
        <p class="description">Выполнить после ручного редактирования файлов на сервере.</p>
        <code>sudo systemctl restart service</code>
    </div>

    <div class="command-card">
        <h2>Посмотреть ВСЕ активные сервисы</h2>
        <p class="description">Эта команда выведет список всех сервисов, которые работают в данный момент</p>
        <code>systemctl list-units --type=service --state=running</code>
    </div>
    <div class="command-card">
        <h2>Выключить навсегда (убрать из автозагрузки)</h2>
        <p class="description">Эта команда сделает так, чтобы сервис не запускался сам при включении приставки</p>
        <code>sudo systemctl disable service</code>
    </div>


</body>
</html>

--- FILE: templates/work.html ---
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>О работе сайта</title>
    <style>
        body { font-family: sans-serif; max-width: 800px; margin: 50px auto; padding: 0 20px; }
        nav { background: #f4f4f4; padding: 10px; margin-bottom: 20px; }
        nav a { margin-right: 15px; text-decoration: none; color: #007bff; }
    </style>
</head>
<body>
    <nav>
        <a href="/">Главная</a>
        <a href="/author">Об авторе</a>
        <a href="/work">О работе сайта</a>
        <a href="/commands">Команды</a>
    </nav>
    <h1>Как работает этот сайт?</h1>
    <ul>
        <li><strong>Железо:</strong> Orange Pi Zero 3</li>
        <li><strong>ОС:</strong> Ubuntu / Debian</li>
        <li><strong>Backend:</strong> Python + FastAPI</li>
        <li><strong>Доступ:</strong> Cloudflare Tunnel + Tailscale</li>
        <li><strong>Автоматизация:</strong> GitHub Actions (CI/CD)</li>
    </ul>
    <h1>Вот так выглядит сервер)</h1>
    <img src="{{ url_for('static', path='/images/photo_2026-03-02_13-45-12.jpg') }}" style="width: 200px; border-radius: 10px;">
</body>
</html>

--- FILE: .github/workflows/deploy.yml ---
name: Deploy to Orange Pi

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Connect to Tailscale
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST_IP }}
          username: ${{ secrets.HOST_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ~/My/webSite
            # 1. Принудительно обновляем код до состояния в GitHub
            git fetch --all
            git reset --hard origin/main
            
            # 2. Обновляем зависимости внутри venv
            # (Убедись, что файл requirements.txt есть в репозитории)
            if [ -f requirements.txt ]; then
              ./venv/bin/pip install -r requirements.txt
            fi
            
            # 3. Перезапускаем системный сервис
            # (Мы разрешили это делать без пароля через visudo)
            sudo systemctl restart my-website.service
            
            # 4. Проверяем статус, чтобы увидеть в логах GitHub, что всё ок
            systemctl is-active my-website.service
